stages:
  - build
  - test

variables:
  DOTNET_VERSION: "8.0"
  CONFIGURATION: "Debug"

before_script:
  - echo "Setting up .NET environment"
  - dotnet --version

build:
  stage: build
  image: mcr.microsoft.com/dotnet/sdk:$DOTNET_VERSION
  script:
    - echo "Compiling the code..."
    - dotnet restore
    - echo "Compile complete."
  artifacts:
    paths:
      - bin/
      - obj/
  rules:
    - if: '$CI_MERGE_REQUEST_ID'

test:
  stage: test
  image: mcr.microsoft.com/dotnet/sdk:$DOTNET_VERSION
  dependencies:
    - build
  script:
    - echo "Running tests..."
    - cd cis-api-legacy-integration-phase-2/cis-api-legacy-integration-phase-2.Test  # Cambia al directorio correcto
    - dotnet test 

  rules:
    - if: '$CI_MERGE_REQUEST_ID'