stages:
  - build
  - test

variables:
  DOTNET_VERSION: "8.0"

before_script:
  - echo "Setting up .NET environment. Using $DOTNET_VERSION .NET version."
  - apt-get update && apt-get install -y wget
  - wget https://packages.microsoft.com/config/debian/12/packages-microsoft-prod.deb -O packages-microsoft-prod.deb
  - dpkg -i packages-microsoft-prod.deb
  - apt-get update && apt-get install -y dotnet-sdk-$DOTNET_VERSION
  - dotnet restore cis-api-legacy-integration-phase-2.Test/cis-api-legacy-integration-phase-2.Test.csproj

build:
  stage: build
  script:
    - echo "Building Project..."
    - dotnet build --configuration Release
  artifacts:
    paths:
      - bin/
      - obj/
  rules:
    - if: '$CI_MERGE_REQUEST_ID'

.test:
  stage: test
  image: mcr.microsoft.com/dotnet/sdk:$DOTNET_VERSION
  dependencies:
    - build
  script:
    - echo "Running tests..."
    - cd cis-api-legacy-integration-phase-2.Test  
    - dotnet test 

  rules:
    - if: '$CI_MERGE_REQUEST_ID'